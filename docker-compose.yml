services:
  db:
    image: postgres:alpine
    container_name: gerenciador-db
    restart: always
    environment:
      - POSTGRES_DB=gerenciador
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=gestao_pass
    volumes:
      - postgres_data:/var/lib/postgresql/data

  db-redis:
    image: redis:alpine
    container_name: gerenciador-redis
    restart: always

  evolution_api:
    image: atendai/evolution-api:v1.8.7
    container_name: gerenciador-evolution
    restart: always
    ports:
      - "8080:8080"
    volumes:
      - evolution_data:/evolution/instances
    environment:
      - SERVER_URL=http://localhost:8080
      - AUTHENTICATION_TYPE=apikey
      - AUTHENTICATION_API_KEY=gq-evolution-secret-key-2026
      - AUTHENTICATION_EXPOSE_IN_FETCH_INSTANCES=true
      - STORE_SAVE_DATA_INSTANCE=true
      - STORE_STORE_SAVE_DATA_INSTANCE=true
      - CONFIG_SESSION_PHONE_VERSION=2.3000.1028355510
      - CONFIG_SESSION_NAME_CLIENT=GestaoQualificacao
      - QRCODE_LIMIT=30
      - WEBHOOK_GLOBAL_ENABLED=false
      - LOG_LEVEL=ERROR
      - LOG_COLOR=false
    depends_on:
      - db-redis

  web:
    build: .
    container_name: gerenciador-backend
    restart: always
    volumes:
      - .:/app
      - static_volume:/app/staticfiles
      - media_volume:/app/media
    ports:
      - "8000:8000"
    environment:
      - DEBUG=True
      - REDIS_URL=redis://gerenciador-redis:6379/0
      - REDIS_HOST=gerenciador-redis
      - REDIS_CACHE_URL=redis://gerenciador-redis:6379/1
      - DB_ENGINE=django.db.backends.postgresql
      - DB_NAME=gerenciador
      - DB_USER=postgres
      - DB_PASSWORD=gestao_pass
      - DB_HOST=gerenciador-db
      - DB_PORT=5432
      - EVOLUTION_API_URL=http://gerenciador-evolution:8080
      - EVOLUTION_API_KEY=gq-evolution-secret-key-2026
    depends_on:
      - db
      - db-redis
      - evolution_api

volumes:
  static_volume:
  media_volume:
  postgres_data:
  evolution_data:
